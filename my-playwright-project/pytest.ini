[pytest]
addopts = --browser chromium --headed
testpaths = tests
# Other options: firefox, webkit

# Run headed (see the browser window):
# addopts = --browser chromium --headed

# Slow down actions for debugging:
# addopts = --browser chromium --headed --slowmo 500